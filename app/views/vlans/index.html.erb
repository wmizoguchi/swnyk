<h1><%= t('.title') %></h1>

<%= link_to 'New Vlan', new_vlan_path, class: 'btn btn-primary' %>
<br>
<%= paginate @vlans %>

<table id='vlans' class='table table-striped table-condensed table-hover'>
  <thead>
    <tr>
      <th>vlan id</th>
      <th>Description</th>
      <th>updated_at</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody class='page'>
    <%= render @vlans %>
  </tbody>
</table>

<%= paginate @vlans %>
